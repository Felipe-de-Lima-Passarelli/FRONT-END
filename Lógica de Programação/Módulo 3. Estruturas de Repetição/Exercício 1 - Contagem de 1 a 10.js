// Crie um programa que exiba no console os números de 1 a 10, utilizando um loop for.

for (let i = 1; i <= 10; i++) {
  console.log(i);
}

// Mostre os números em ordem decrescente (de 10 a 1).

for (let index = 10; index >= 1; index--) {
  console.log(index);
}
