// Peça ao usuário (ou defina no código) os valores de largura e altura de um retângulo. Calcule e exiba a área desse retângulo. Fórmula: área = largura * altura

/* Pedindo ao usuário os valores
let largura1 = Number(prompt("Digite o valor da largura em metros"));
let altura1 = Number(prompt("Digite o valor da altura em metros"));
console.log(largura1 * altura1); */

//Definindo no código
let largura2 = 2.5;
let altura2 = 1.2;
console.log(largura2 * altura2);

// Adapte para calcular também o perímetro. Fórmula do perímetro: perímetro = 2 * (largura + altura)

console.log(2 * (largura2 + altura2));
