// Crie um objeto chamado pessoa com as seguintes propriedades: nome, idade e profissão

const pessoa = {
  nome: "Felipe",
  idade: 25,
  profissao: "Estudante",
};

console.log(pessoa);

// Adicione uma nova propriedade chamada cidade dinamicamente (após criar o objeto).

pessoa.cidade = "Brasil";
console.log(pessoa);
