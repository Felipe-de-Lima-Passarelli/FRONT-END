// Crie uma função anônima que recebe dois números e retorna a soma. Atribua essa função a uma variável chamada soma.

const soma = function (a, b) {
  return a + b;
};

// Chame soma(5, 8) e mostre o resultado no console.

console.log(soma(5, 8));
