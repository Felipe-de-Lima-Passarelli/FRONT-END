const nome = "Felipe de Lima Passarelli";
const arrayNome = nome.split(" "); //Transforma uma String em um Array
const nomeArray = arrayNome.join(" "); //Transforma um Array em uma String

console.log(arrayNome);
console.log(nomeArray);
